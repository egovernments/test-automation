actions:
  initiate: INITIATE
  approve: APPROVE
  isActiveUom: true
  active: true

channel:
  counter: COUNTER

status:
  status: successful

building:
  single: SINGLE

usageType:
  usageTypeMajor: GROUP_A_RESIDENTIAL

ownerShip:
  ownerShipMajorType: INDIVIDUAL

gender:
  - MALE
  - FEMALE

errorMessages:
  invalidBuildType: should be equal to one of the allowed values
  invalidUsage: valid_buildingType
  invalidUsageType: should pass "valid_buildingType" keyword validation
  invlidUom: valid_uom
  invalidUomCode: should pass "valid_uom" keyword validation
  invalidUomActive: should be boolean
  invalidMobileNumber: should match pattern "^[6789][0-9]{9}$"
  invalidFirestation: should pass "valid_firestation" keyword validation
  invalidMObileNo: must match "(^$|[0-9]{10})"
  billingSlabError: No Billing slabs found!
  relationShipError: should be string
  invalidAction: Action APPROVE not found in config for the businessId