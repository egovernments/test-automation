{
    "RequestInfo": {
      "authToken": "#(authToken)"
    },
    "paymentWorkflows": [
      {
        "reason": "#(reason)",
        "action": "#(action)",
        "tenantId": "#(tenantId)",
        "paymentId": "#(paymentId)"
      }
    ]
  }